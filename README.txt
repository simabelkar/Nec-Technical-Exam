
install java jdk
edit system variables: 
	* Path = appent the java jdk bin folder location
	* JAVA_HOME = set with java jdk folder location

To install Apache Maven on Windows, you just need to download the Maven’s zip file from: https://maven.apache.org/download.cgi
Unzip it to the directory you wish to install, and configure the Windows environment variables:
	* Path = append with the maven bin folder location
	* M2_HOME = set with maven folder location
	* MAVEN_HOME = set with maven folder location
more details about installation and setup process can be found here: https://www.mkyong.com/maven/how-to-install-maven-in-windows/
	
	
Tools Used:
JDK 1.8
Maven 3.5.0
Windows 10